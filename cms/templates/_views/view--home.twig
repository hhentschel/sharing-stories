{%- import '_macros/atomic' as atoms -%}

<div class="container bg-amber-100 flex flex-wrap p-8 rounded mt-6 mb-12">


    <div class="mb-2 flex flex-wrap w-6/12 gap-8 text-lg">
         {{- atoms.atom('image', 'hero',  {
                image: entry.imageHero.eagerly().collect.first,
                sizes: '40vw',
                placeholder: 'silhouette',
                loadingStrategy: 'eager',
            }) -}}
    </div>

    <div class="mb-2 flex flex-wrap w-6/12 text-xl">
        {{ entry.longformContent }}
    </div>
</div>

<div class="container ">

    {{ entry.title }}

    <div class="">
        {{ entry.HomeContent }}
        {# view--home #}

            {% for block in entry.contentBuilder.all() %}
            {% switch block.type %}

                {% case "cta" %}
                {{ block.render() }}

                {% case "richText" %}
                {{ block.render() }}

                {% case "images" %}
                {{ block.render() }}

                {% case "featureList" %}
                {{ block.render({ loop, themeColor: "lightblue" }) }}

                {% case "relatedEntries" %}
                {{ block.render() }}

                {% case "table" %}
                {{ block.render() }}

                {% default %}


            {% endswitch %}

            {% endfor %}

    </div>
</div>


