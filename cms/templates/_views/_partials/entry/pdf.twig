{%- import '_macros/atomic' as atoms -%}

{% set pdf = entry.pdf.one() %}

<div class="">
<h3 class="mt-6 mb-4 text-3xl">{{ entry.title }}</h3>
<embed src="{{ pdf.url }}" type="application/pdf" width="100%" height="1200px" />
</div>
