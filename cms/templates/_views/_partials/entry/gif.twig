{%- import '_macros/atomic' as atoms -%}

{% set gif = entry.imageContentGif.one() %}


<div class="">

  <img
    src="{{gif.url}}"
    class="object-cover aspect-3/2"
    fetchpriority="high"
  >


{# {% for image in entry.imageContentGif %}
{{- atoms.atom('image', 'gif',  {
    image: gif.url,
    sizes: '35vw',
    placeholder: 'image',
    caption: entry.caption,
    loadingStrategy: 'lazy',

}) -}}
{% endfor %} #}

</div>